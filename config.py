WHERE_TO_TAP = 542, 761  # X Y coordinate
REST_BETWEEN_EACH_TAP = (0.01, 0.1)  # a random number between these, in seconds
MAXIMUM_NUMBER_OF_TAPS = 500
COOLDOWN_TIME = 500  # in seconds
# cooldown and number of taps are supposed to be exactly what the bot wants
# the app taps to the maximum amount
# and there's an anti-bot-detection method applied by default

KEEP_SCREEN_ON = True
# no need for the two bellow if it's False
WHERE_TO_TAP_FOR_WAKING = 1, 1200
REST_BETWEEN_EACH_WAKE_TAP = 20  # in seconds
